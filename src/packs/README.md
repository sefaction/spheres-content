# Canonical packs

One reviewed JSON document per file, in a registered pack directory. This
foundation deliberately registers no packs until the content plan is agreed.
See `docs/CONTENT_PIPELINE.md` for the format and validation contract.
