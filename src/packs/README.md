# Canonical packs

One reviewed JSON document per file, in a registered pack directory. The initial
pilot registers items, feats, classes, and Guile talents. Preserve IDs and source
keys in `config/identities.json`; canonical JSON is the editing source.
See `docs/CONTENT_PIPELINE.md` for the format and validation contract.
